---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tags: 1.13.7

inputs:
  - name: dp-download-service
    path: dp-download-service

outputs:
  - name: build

run:
  path: dp-download-service/ci/scripts/build.sh
